{
  "evidence": "SQL injection found in login form. The payload admin OR 1=1 bypassed authentication and granted access without valid credentials. This indicates the application is vulnerable to SQL injection attacks.",
  "severity": "Critical"
}
